name: Workflow Branch Test

on:
  workflow_dispatch:
    inputs:
      logLevel:
        description: 'Log level'
        required: false
        default: 'warning'
      test:
        description: 'Test'
        required: false
        default: 'warning'

jobs:
  printInputs:
    runs-on: ubuntu-latest
    steps:
    - name: Echo hi
      run: echo hi
